apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "SapAct.fullname" . }}-test-connection"
  labels:
    {{- include "SapAct.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "SapAct.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
